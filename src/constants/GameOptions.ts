import VectorPos from "../interfaces/universal/VectorPos"

export const PieceScale : VectorPos = {
    x: 0.35,
    y: 0.35
};

// Board Options
export const TileSize: VectorPos = {
    x: 72,
    y: 72
};

export const GapSize: VectorPos = {
    x: 80,
    y: 80,
};